interface:
  display_name: "EURAXESS Jobs Scraper | Full Details & AI Enrichment"
  short_description: "Find EURAXESS jobs as source-faithful structured records"
  default_prompt: "Use $euraxess-enrich-translate-normalize-scraper to find or validate EURAXESS jobs while preserving the complete structured record."

dependencies:
  tools:
    - type: "mcp"
      value: "apify"
      description: "Inspect compatibility, call EURAXESS through latest and retain immutable build evidence, validate factual RUN-SUMMARY, and retrieve its dataset"
      transport: "streamable_http"
      url: "https://mcp.apify.com?tools=fetch-actor-details,call-actor,get-actor-run,get-dataset-items,get-key-value-store-record"
